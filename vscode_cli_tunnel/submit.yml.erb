batch_connect:
  template: "basic"
script:
  copy_environment: no
  native:
    - "--mem=<%= memory %>G"
    - "--time=<%= bc_num_hours %>:00:00"
    - "-N 1"
    - "-n <%= bc_num_slots.to_i %>"
    - "--partition=test"  
    - "-J vscodectli"
